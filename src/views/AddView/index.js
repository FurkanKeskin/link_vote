import React from "react";
import AddViewContainer from "./components/AddContainer/";

const AddView = (props) => {
    
    return (
            <AddViewContainer />
    )
};

export default AddView;